{
  "name": "xylab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "XYLab — 玄域机制公式实验台（协议 / 运行时 / 监控 / UI）",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "governance": "node scripts/governance-guard.mjs",
    "verify": "npm run governance && npm run typecheck && npm run test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
